namespace Sherlock.Apps.Model
{
    public enum ReservationEdges { has, have };
    public enum PropertyEdges { have, contain };
    public enum GuestEdges { };
    public enum EmployeeEdges { assign, approve, manage };
    public enum EmailRequestEdges { request };
}
